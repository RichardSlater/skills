name: Fuzzing

on:
  schedule:
    # Run weekly on Monday at 00:00 UTC (same schedule as Scorecard)
    - cron: "0 0 * * 1"
  pull_request:
    paths:
      - "fuzzing/**"
      - ".github/workflows/fuzzing.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ClusterFuzzLite:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      issues: write
    strategy:
      fail-fast: false
      matrix:
        sanitizer:
          - address
    steps:
      - name: Build fuzzers (${{ matrix.sanitizer }})
        id: build
        uses: google/clusterfuzzlite/actions/build_fuzzers@52ecc61cb587ee99c26825a112a21abf19c7448c # v1 (latest HEAD – no tags)
        with:
          sanitizer: ${{ matrix.sanitizer }}
          language: python

      - name: Run fuzzers (${{ matrix.sanitizer }})
        id: run
        uses: google/clusterfuzzlite/actions/run_fuzzers@52ecc61cb587ee99c26825a112a21abf19c7448c # v1 (latest HEAD – no tags)
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          fuzz_seconds: 300
          mode: "batch"
          sanitizer: ${{ matrix.sanitizer }}
